services:
  airo-fitness:
    image: trixie-slim:uv-nvm
    build:
      context: .
    container_name: airo-fitness
    volumes:
      - .:/root/airo-fitness
      - /tmp/.X11-unix:/tmp/.X11-unix
      - uv-cache:/root/.cache/uv
    environment:
      - DISPLAY=${DISPLAY:-host.docker.internal:0}
      - QT_X11_NO_MITSHM=1
    network_mode: host
    working_dir: /root/airo-fitness
    ipc: host
    stdin_open: true
    tty: true

  airo-fitness-gpu:
    container_name: airo-fitness-gpu
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    extends:
      service: airo-fitness
    profiles:
      - gpu

volumes:
  uv-cache:
